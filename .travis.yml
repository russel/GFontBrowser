language: d
sudo: true
dist: xenial

d:
  # - dmd # Link failure
  - ldc
  # - gdc # Uses gdc 4.8.5 which is just too old.

before_script:
  - sudo apt-get -qq update
  - sudo apt-get install -y libgtk-3-0 libfontconfig1 libpango1.0
